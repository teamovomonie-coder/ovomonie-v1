# Local environment variables for development
# Copy this file to `.env.local` and fill in your real values. Do NOT commit secrets.

AUTH_SECRET=change-me-to-a-strong-random-string

NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Path to your service account JSON for server-side Admin SDK (optional). If set, ensure the file is **not** checked in.
# Example: C:\Users\user\secrets\service-account.json
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json

# Alternative: if you prefer not to use a file, set NEXT_PUBLIC_FIREBASE_PROJECT_ID and ensure ADC is available.
# For local testing with the Emulator Suite, you can set:
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
