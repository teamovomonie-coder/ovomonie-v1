/**
 * Import function triggers from their respective submodules:
 *
 * import {onCall} from "firebase-functions/v2/https";
 * import {onDocumentWritten} from "firebase-functions/v2/firestore";
 *
 * See a full list of supported triggers at https://firebase.google.com/docs/functions
 */

/**
 * Legacy Firebase Cloud Functions have been archived.
 *
 * The project migrated to Supabase for all server-side database operations.
 * Keeping Firebase function sources here may confuse CI or cause build failures
 * because `firebase-admin` is intentionally removed from `functions/package.json`.
 *
 * If you need to migrate these triggers to Supabase Edge Functions or another
 * serverless platform, follow the migration notes in `docs/SUPABASE_MIGRATION.md`.
 */

// Placeholder export to keep the functions package valid during migration.
export const archived = true;
