rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows read and write access to all documents by anyone.
    // This is NOT secure and should only be used for development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
